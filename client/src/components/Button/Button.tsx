import React from 'react';

type ButtonProps = {
};

const Button: React.FC<ButtonProps> = () => {

    return (
        <div>
            <h1>Button</h1>
        </div>
    );
};

export default Button;
