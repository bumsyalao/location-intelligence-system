import React from 'react';

type DashboardProps = {
    // Additional props specific to Dashboard
};

const Dashboard: React.FC<DashboardProps> = () => {

    return (
        <div>
            <h1>Dashboard</h1>
        </div>
    );
};

export default Dashboard;
